---

machine:
  timezone: UTC
  services:
    - docker
  environment:
    KITCHEN_YAML: .kitchen.ci.yml

dependencies:
  pre:
    - gem install bundler
  bundler:
    without:
      - workstation

test:
  override:
    - bundle exec kitchen test
