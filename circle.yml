---

machine:
  timezone: UTC
  services:
    - docker
  environment:
    KITCHEN_YAML: .kitchen.ci.yml

dependencies:
  bundler:
    without:
      - workstation

test:
  override:
    - kitchen test
